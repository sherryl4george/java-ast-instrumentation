compile{
  root = "/home/maithreyi/Desktop/cs474.anttest"
  # Everything below this line is relative to the
  # project root
  srcDir = "src"
  targetDir = "target"
  jarFolder = "jars"
  buildFile = "build.xml"
}

run {
  # Provide the qualified main class
  mainClass = "com.uic.edu.App"
  # Provide input parameters
  arguments = [
    [1,2,"mr"],
    [3,4,"st"],
    [4,5,"abc"]
  ]
}

resources{
    astparser = "astparser"
    oldSrc = "oldSrc"
}
